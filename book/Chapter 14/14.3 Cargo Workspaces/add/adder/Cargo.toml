[package]
name = "adder"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html


# explicitly specify that this crate depends on the add_one library crate
[dependencies]
add_one = { path = "../add_one" }
rand = "0.8.5"
